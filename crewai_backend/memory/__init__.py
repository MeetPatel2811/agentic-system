# ---------------------------------------------------------
# This file marks `crewai_backend.memory` as a package.
# It contains both SQLite (symbolic history) and vector
# memory (semantic memory via Chroma + OpenAI embeddings).
# ---------------------------------------------------------
